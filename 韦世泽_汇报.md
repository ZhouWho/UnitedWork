

### 完成内容

* 对 6 月中下旬训练效果较好的 `G1ShuffleLocomotionAgent` 19 版本进行整理。
* 明确当前最佳移动模型以 **Strict 19 Clean 基线版** 为核心，不再混入后续失败实验中的 DirectionFix、SafeMove、FirstStep、NoFootHang 等逻辑。
* 保留原始 19 版本的基本训练结构：

  * 140 维观测；
  * 29 维连续动作；
  * 下肢 0\~11 维用于移动控制；
  * 腰部与上肢保持参考姿态锁定；
  * 使用 `leg\_neutral\_deg` 作为下肢动作中心；
  * 使用随机速度命令训练全向碎步。
* 训练结果显示，模型在 W/S/A/D 移动方面已经具备一定稳定性，尤其在键盘输入与训练命令范围一致后，前进、后退和左右移动明显稳定。
* 当前主要不足仍集中在 Q/E 转向能力较弱，后续需要单独针对 yaw 方向进行训练设计。

### 图片插入

<img width="1672" height="941" alt="Strict 19 Clean 技术路线图" src="https://github.com/user-attachments/assets/06dacebd-5c75-4d35-b4e9-759c24a78697" />


\*\*图片说明：\*\*展示 Strict 19 Clean 基线版的整体技术路线，包括参考姿态读取、140 维观测、29 维残差动作、基础奖励、命令采样和 ONNX 导出部署流程。


<img width="1164" height="1275" alt="Strict 19 Clean 脚本 Inspector 参数截图" src="https://github.com/user-attachments/assets/75522012-c696-4875-8faf-c1dcea2a2854" />




\*\*图片说明：\*\*展示 Strict 19 Clean 训练脚本的 Inspector 参数。重点展示 `motion\_id`、`target\_stand\_frame`、`leg\_neutral\_deg`、`action\_scale\_deg`、奖励权重、终止条件和键盘推理相关设置。

!\[Strict 19 Clean 脚本 Inspector 参数截图](./images/Strict 19 Clean 脚本 Inspector 参数截图.png)

### 成果视频插入


https://github.com/user-attachments/assets/1bd18bfe-a8d1-442a-bc99-2b0b8dc5eedd

\*\*视频说明：\*\*展示 Strict 19 Clean 模型在键盘控制下的前进、后退、左右移动和基本转向测试结果。该视频作为 19 版本 PPO 移动训练最终成果展示。

### 关键参数记录

```text
Behavior Name = G1Shuffle
Vector Observation = 140
Continuous Actions = 29

cmd\_x\_range = 0.4
cmd\_y\_range = 0.2
cmd\_yaw\_range = 0.3
zero\_cmd\_prob = 0.1

keyboard:
W/S = 0.4
A/D = 0.2
Q/E = 0.3
```

\---

## 2026.06.18：完成 Strict 19 Clean 回退与启动逻辑清理

### 完成内容

* 对此前多个 PPO 移动训练脚本版本进行回退和清理。
* 确认后续训练基线应回到原始 19 版本，避免使用已经证明存在错误局部最优的版本。
* 检查并移除以下后续实验逻辑：

  * DirectionFix 方向强化逻辑；
  * SafeMove 安全门控逻辑；
  * FirstStep 单脚抬起奖励；
  * NoFootHang 防悬空逻辑；
  * 强制 Start 预传送；
  * 运行时强制回写参数。
* 重新整理 Strict 19 Clean 版本，保证运行逻辑与原始 19 版本一致，只作为可控稳定基线使用。
* 通过代码检查确认：

  * 无 `force\_19\_clean\_defaults\_on\_start`；
  * 无 `preload\_19\_pose\_on\_start`；
  * 无 `motion\_csv\_override`；
  * `motion\_id = 16` 和 `target\_stand\_frame = 530` 作为默认值保留；
  * `leg\_neutral\_deg` 保持 19 旧值。

### 图片插入

<img width="2728" height="1498" alt="19版站立CSV_数据示意图" src="https://github.com/user-attachments/assets/4e080fa0-ad10-45f4-9c61-67dcf9854a55" />


\*\*图片说明：\*\*展示 19 版本站立参考 CSV 的数据结构，包括 root 7 列、下肢/腰部关节角度和上身/手臂关节角度。该图用于说明 Strict 19 Clean 的参考站姿来源。



### 关键代码片段

```csharp
public int motion\_id = 16;
public int target\_stand\_frame = 530;
public bool target\_frame\_is\_itp\_100hz = true;

public float\[] leg\_neutral\_deg = new float\[15]
{
    -10f, 4f, 0f, 25f, -15f, 0f,
    -10f, -4f, 0f, 25f, -15f, 0f,
    0f, 0f, 3f
};
```

\---

## 2026.06.15：分析移动 PPO 奖励区间与训练日志

### 完成内容

* 对 Strict 19 奖励结构进行分析，明确训练分数和实际运动能力之间的关系。
* 计算出当前奖励的大致区间：

  * 纯站稳不动理论分约 90；
  * 100 左右不能说明一定学会移动；
  * 120 以上开始说明具有移动收益；
  * 140 以上较值得挂载测试；
  * 160 左右说明模型很可能已经超过单纯站稳区间。
* 对 `loco\_f1\_4` 训练日志进行判断：

  * 训练从 3677 万步附近恢复；
  * Mean Reward 从 9.854 逐步上升到 160.072；
  * 说明模型正在从不稳定状态恢复到较稳定移动状态。
* 根据日志判断当前训练不是失败趋势，建议继续训练到下一个 ONNX 导出点并进行 Play 测试。

### 关键数据记录

```text
Step 36780000: Mean Reward = 9.854
Step 36960000: Mean Reward = 53.284
Step 37060000: Mean Reward = 76.021
Step 37160000: Mean Reward = 116.859
Step 37220000: Mean Reward = 127.253
Step 37340000: Mean Reward = 160.072
```

### 分析结论

```text
90 分附近：可能只是站稳。
120 分以上：开始具有移动收益。
140 分以上：值得挂载测试。
160 分左右：说明模型已经明显超过纯站稳区间。
```

\---

## 2026.06.10：整理失败训练版本与局部最优问题

### 完成内容

* 对 6 月上旬的 PPO 移动训练失败经历进行总结。
* 重点分析 allway7 等实验中的错误局部最优现象：

  * 左右移动时机器人偏向一侧；
  * 一只脚站定不动，另一只脚缓慢悬空；
  * 左右反复按键时出现类似鸭子步的前蹭；
  * 分数上涨但实际动作变差。
* 分析原因认为：过早加入单脚支撑、落脚奖励、抬脚奖励，会诱导策略用“悬空一只脚”方式骗取奖励。
* 因此后续训练中不再采用 FirstStep 类强抬脚奖励，不再直接奖励单脚悬空。

### 失败经验总结

```text
失败版本现象：
Mean Reward 上升，但动作质量下降。

主要问题：
1. 单脚支撑奖励过强；
2. 抬脚奖励缺少稳定落脚约束；
3. 策略学会偏向一侧，而不是真正左右移动；
4. 分数不能直接代表动作质量。
```

\---

## 2026.06.04：尝试 DirectionFix 与 SafeMove 方向强化方案

### 完成内容

* 针对早期模型后退相对可行、前进和左右较弱的问题，尝试设计方向强化奖励。
* 加入方向进度奖励，试图让模型更明确地区分前进、后退、左右和转向。
* 尝试加入安全移动门控，减少模型在移动时靠身体倾斜或拖脚获取奖励。
* 实际训练和测试中发现：

  * 后退仍然相对容易；
  * 前进仍容易前倾；
  * 左右移动仍存在拖脚和身体侧偏；
  * 转向仍然无法稳定完成；
  * 分数增长并不能稳定转化为动作质量提升。
* 因此决定不再继续扩大 DirectionFix/SafeMove 体系，而是回到更简单的 19 基线。

### 失败经验总结

```text
方向奖励过强时，策略更想快速产生速度，而不是先保持稳定。
安全门控过强时，策略又容易变得不敢移动。
```

\---

## 2026.05.28：完成 G1ShuffleLocomotionAgent 小碎步 PPO 训练主线搭建

### 完成内容

* 在前期模仿学习基础上，开始将任务从“跟随 CSV 动作”转向“根据速度命令自主移动”。
* 搭建 `G1ShuffleLocomotionAgent` PPO 强化学习脚本主线。
* 训练目标调整为：

  * 输入速度命令 `cmd\_x`、`cmd\_y`、`cmd\_yaw`；
  * 机器人根据命令完成前进、后退、左右移动和转向；
  * 通过速度跟踪、净位移、站稳、脚部接触和姿态稳定奖励进行学习。
* 观测保持 140 维，动作保持 29 维，便于后续和动作模型、控制器进行统一衔接。
* 初步形成移动模型训练框架。

### 图片插入

<img width="595" height="429" alt="PPOstirct19cleanBehaviour参数配置截图" src="https://github.com/user-attachments/assets/47e603bd-d620-4cfb-93e3-9218502f8adc" />

\*\*图片说明：\*\*展示 `G1Shuffle` 的 Behavior Parameters 设置，包括 Behavior Name、140 维观测、29 维连续动作、模型挂载和推理参数。

<img width="772" height="1043" alt="PPOstirct19clean移动训练YAML配置截图" src="https://github.com/user-attachments/assets/4d2143b4-7f10-4879-ade0-de100fa24083" />


\*\*图片说明：\*\*展示 PPO 移动训练使用的 YAML 参数配置，包括 `cmd\_x\_range = 0.4`、`cmd\_y\_range = 0.2`、`cmd\_yaw\_range = 0.3`、`zero\_cmd\_prob = 0.1`。



### 关键代码片段

```csharp
\_cmd = new Vector3(
    Random.Range(-cmd\_x\_range, cmd\_x\_range),
    Random.Range(-cmd\_y\_range, cmd\_y\_range),
    Random.Range(-cmd\_yaw\_range, cmd\_yaw\_range)
);
```

```csharp
float baseDeg = leg\_neutral\_deg\[i];
jointTargetDeg\[i] = baseDeg + action\_scale\_deg\[i] \* actionFiltered\[i];
SetJointTargetDeg(jh\[i], jointTargetDeg\[i]);
```

\---

## 2026.05.18：从模仿移动转向 PPO 自主移动训练

### 完成内容

* 总结前期使用 CSV 移动片段训练的不足。
* 发现单纯模仿后退、前进或左右 CSV 时，模型容易只学到片段中的局部姿态，而不是可控启停的移动能力。
* 在动作切换控制中，移动模型和动作模型之间难以通过硬切换或平滑切换保持平衡。
* 因此决定将移动任务改为 PPO 强化学习形式，由策略根据速度命令自主产生小碎步。
* 明确移动模型只负责：

  * 稳定站姿；
  * 前后移动；
  * 左右移动；
  * 基础转向；
  * 为拳击/踢腿动作模型提供可接管的移动底座。

\---

## 2026.05.08：测试移动 CSV 模仿训练与模型切换问题

### 完成内容

* 尝试使用前进、后退、左右移动等移动 CSV 片段进行模仿学习训练。
* 将站立帧和移动帧组合成“站立—移动—站立”的序列，用于训练移动动作模型。
* 发现模型能够在部分片段中模仿出短时间移动，但存在明显问题：

  * 移动启停难以控制；
  * 长时间按键无法稳定持续移动；
  * 模型之间切换时容易失去平衡；
  * 移动模型不能自然和拳击动作模型衔接。
* 该阶段结果推动后续改用 PPO 命令式移动训练。

### 成果视频插入





https://github.com/user-attachments/assets/752ea0a3-af40-4305-8481-69d61345b24a






\*\*视频说明：\*\*展示早期使用移动 CSV 进行模仿训练得到的左右移动效果。该视频作为后续 PPO 移动训练成果的对比材料。



## 2026.04.22：完成前期 G1mimic1Agent1\_fixed 移动模仿训练阶段整理

### 完成内容

* 使用 `G1mimic1Agent1\_fixed` 对简单拳击机器人移动动作进行模仿学习训练。
* 训练方式与 C、D 成员前期拳击姿势模仿训练原理一致：

  * 读取 CSV 动作数据；
  * 通过 `motion\_id` 选择动作文件；
  * 使用 `frame\_start` 和 `frame\_end` 指定移动片段；
  * 将 30Hz 原始数据插值到 100Hz；
  * 策略输出关节残差动作；
  * 通过姿态相似度、速度相似度、root 跟踪和稳定性奖励进行训练。
* 初步训练出简单左右移动动作片段，为后续 PPO 移动训练提供了对照基线。

### 图片插入



<img width="754" height="1123" alt="旧版inspector模仿训练截图" src="https://github.com/user-attachments/assets/7ceb71ed-ffd0-4587-b09f-022ecf0d41f6" />



\*\*图片说明：\*\*展示 `G1mimic1Agent1\_fixed` 的 Inspector 参数，包括 `train`、`replay`、`motion\_id`、`frame\_start`、`frame\_end`、`loop\_playback`、`actionSmooth`、`actionScale`、`stiffness`、`damping`、奖励权重和终止条件。



### 成果视频插入






https://github.com/user-attachments/assets/e3fd916c-332f-4d4e-bfbb-1fef2edf246f





\*\*视频说明：\*\*展示 `G1mimic1Agent1\_fixed` 对左右移动 CSV 片段的早期模仿训练成果。



## 2026.03.26：启动简单拳击机器人移动模仿训练

### 完成内容

* 在 Unity 2022 中接入 `G1mimic1Agent1\_fixed`。
* 整理用于移动模仿训练的 CSV 动作片段。
* 配置 ML-Agents Behavior Parameters，确认观测维度、动作维度和训练模式。
* 使用 `train = true` 进入训练，使用 `replay = true` 检查 CSV 参考动作。
* 通过初步训练确认机器人可以读取 CSV、插值并执行移动片段的基础动作跟踪。



截至 2026.06.21， 主要完成了 G1 机器人移动训练从早期 CSV 模仿学习到 PPO 强化学习的技术路线探索。前期基于 `G1mimic1Agent1\_fixed` 完成简单移动片段模仿训练，验证了 CSV 读取、插值、动作片段跟踪和基础奖励结构。后期重点转向 `G1ShuffleLocomotionAgent` 的 PPO 小碎步训练，经历了 DirectionFix、SafeMove、FirstStep 等多轮失败尝试，并最终回到 Strict 19 Clean 稳定基线。当前成果是获得了可用于前后和左右移动的全向碎步基础模型，同时明确了转向能力仍需后续专门优化。
